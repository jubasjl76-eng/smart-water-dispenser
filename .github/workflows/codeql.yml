name: CodeQL
on:
  push:
    branches: [main, development]
  pull_request:
  schedule:
    - cron: '6 7 * * 1' # Mondays

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  codeql:
    uses: jubasjl76-eng/smart-pet-ci/.github/workflows/codeql.yml@main
    with:
      languages: '["c-cpp"]'
