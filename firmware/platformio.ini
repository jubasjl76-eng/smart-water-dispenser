; Smart Pet Water Dispenser firmware — built on smart-pet-device-sdk.
;
;   pio run -d firmware
;   wokwi-cli firmware --scenario firmware/water.test.yaml --timeout 20000
;
; The pre-SDK single-file firmware is kept as smart-water-dispenser.legacy.cpp
; and is not compiled (build_src_filter below).

[platformio]
src_dir = .

[env:esp32dev]
platform = espressif32 @ ^6.9.0
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 921600
; two ~1.9 MB app slots so signed A/B OTA has room
board_build.partitions = min_spiffs.csv

build_src_filter = +<main.cpp>
build_unflags = -std=gnu++11 -std=gnu++14
build_flags =
    -std=gnu++17
    -DCORE_DEBUG_LEVEL=2
    -DMQTT_MAX_PACKET_SIZE=1024

lib_deps =
    https://github.com/jubasjl76-eng/smart-pet-device-sdk.git#18ad23b7eea5a8b7a65e248427cd52d4d78fb8cd
    bblanchon/ArduinoJson @ ^7.0.0
    madhephaestus/ESP32Servo @ ^3.0.5
